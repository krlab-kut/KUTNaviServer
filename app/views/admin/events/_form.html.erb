<%= form_for(@events, :url => { :controller => :events, :action => :create }) do |f| %>
<% if @events.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@events.errors.count, "error") %> prohibited this practice from being saved:</h2>
    <ul>
    <% @events.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, size: "60*10" %>
  </div>
  <div class="field">
    <%= f.label :place %><br>
    <%= f.text_field :place %>
  </div>
  <div class="field">
    <%= f.label :start_at %><br>
    <%= f.datetime_select :start_at, {}, class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :start_at %><br>
    <%= f.datetime_select :end_at, {}, class: 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
